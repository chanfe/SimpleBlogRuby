<h1>Blog Post</h1>
<% @posts.each do |post| %>
    <div class="card card-header">
        <h3> <%= post.title %></h3>
        <p> <%= post.body %></p>
        <%= link_to "Read More", post_path(post), :class => 'btn btn-outline-secondary'%>
    </div>
    
<%end%>